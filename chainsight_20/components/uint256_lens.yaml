# yaml-language-server: $schema=https://raw.githubusercontent.com/horizonx-tech/chainsight-cli/main/resources/schema/algorithm_lens.json
version: v1
metadata:
  label: u128 Lens
  type: algorithm_lens
  description: "Get last_snapshot_value from a snapshot indexer and convert it into u128 with 6 decimals so as to be used by EVM contracts."
  tags:
    - uint128
datasource:
  methods:
    - id: index_indexer
      identifier: "get_last_snapshot_value : () -> (LensValue)"
cycles: null
