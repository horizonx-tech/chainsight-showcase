# yaml-language-server: $schema=https://raw.githubusercontent.com/horizonx-tech/chainsight-cli/main/resources/schema/algorithm_lens.json
version: v1
metadata:
  label: u128 Lens
  type: algorithm_lens
  description: "Get the latest Chainsight20 index from indexer and convert it into u128 so as to be used by EVM contracts."
  tags:
    - Chainsight 20
datasource:
  methods:
    - id: index_indexer
      identifier: "get_last_snapshot_value : () -> (LensValue)"
cycles: null
