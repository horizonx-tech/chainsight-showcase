# yaml-language-server: $schema=https://raw.githubusercontent.com/horizonx-tech/chainsight-cli/main/resources/schema/algorithm_lens.json
version: v1
metadata:
  label: u128 Lens
  type: algorithm_lens
  description: "Converts an index value from yahoo.com to u128 so as to be used by the EVM relayer with 6 decimals precision."
  tags:
    - u128
datasource:
  methods:
    - id: sp_global_reit_indexer_https
      identifier: "get_last_snapshot_value : () -> (SnapshotValue)"
      candid_file_path: null
cycles: null
